# Core dependencies
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Telegram bot (async)
pyTelegramBotAPI>=4.14.0

# MQTT client (async)
aiomqtt>=2.0.0
aiohttp

# Protobuf / Meshtastic parsing
meshtastic>=2.4.0
protobuf>=4.24.0

# Type hints
typing-extensions>=4.8.0

# Optional: цветной вывод в sniffer
pygments>=2.14.0

# Optional: для декомпрессии Unishox2 (TEXT_MESSAGE_COMPRESSED_APP)
# Пропущено из requirements из-за отсутствия готовых колёс; используем опциональный импорт

